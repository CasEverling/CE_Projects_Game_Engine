template < class T>
class VectorXD {
    private:
        T* values;
        virtual static size;
    public:
        virtual VectorXD
        virtual bool operator==(const Shape& other) const = 0;

};