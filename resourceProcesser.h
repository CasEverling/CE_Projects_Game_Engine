

class ResourceProcesser {
    public:
        ResourceProcesser();
        // Takes a list of resources and after a given Time, creates another list of items
        // Shop Order system can be made as a resource provcessor that receives nothing
};